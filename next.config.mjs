/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.ibb.co",
      "i.ibb.co.com",
      "lh3.googleusercontent.com",
      "localhost",
      "localhost:8080",
    ], // Add the hostname here
  },
};

export default nextConfig;
